diff --git a/node_modules/react-native-tflite/react-native-tflite.podspec b/node_modules/react-native-tflite/react-native-tflite.podspec
new file mode 100644
index 0000000..4b6212e
--- /dev/null
+++ b/node_modules/react-native-tflite/react-native-tflite.podspec
@@ -0,0 +1,19 @@
+require 'json'
+
+package = JSON.parse(File.read(File.join(__dir__, 'package.json')))
+
+Pod::Spec.new do |s|
+  s.name         = 'react-native-tflite'
+  s.version      = package['version']
+  s.summary      = package['description']
+  s.description  = package['description']
+  s.homepage     = package['repository']['url']
+  s.license      = 'MIT'
+  s.author       = package['author']
+  s.source       = { :git => package['repository']['url'], :tag => "v#{s.version}" }
+  s.platform     = :ios, '9.0'
+  s.source_files = 'ios/**/*.{h,m,mm}'
+  s.requires_arc = true
+
+  s.dependency 'React-Core'
+end
